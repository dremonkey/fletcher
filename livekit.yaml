port: 7880
bind_addresses:
  - ""
rtc:
  node_ip: 100.87.219.109 # NixOS Tailscale IP — forces ICE candidates to use this routable address
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
room:
  departure_timeout: 120 # 2 minutes — must match DEPARTURE_TIMEOUT_S in apps/mobile/.env (BUG-015)
logging:
  level: info
