# T29.04 — ACP Handshake Validation

**Status:** Not started
**EPIC:** [29-relay-claude-code](./EPIC.md)
**PRD Requirement:** FR3
**Depends on:** T29.02

## Objective
Validate the full ACP handshake sequence with Claude Code and document its capability and session behavior.

## Details
- Test the `initialize` -> `initialized` -> `session/new` handshake with Claude Code
- Document the `agentCapabilities` and `agentInfo` response shapes returned by Claude Code
- Test `_meta.session_key` passthrough behavior across all three scenarios from the architecture doc
- Extract and use `sessionId` from `session/new` response for subsequent `session/prompt` calls

## Acceptance Criteria
- [ ] `initialize` -> `initialized` -> `session/new` completes without error
- [ ] `agentCapabilities` and `agentInfo` shapes are documented
- [ ] `_meta.session_key` passthrough works correctly for all three scenarios
- [ ] `sessionId` is correctly extracted and reused for prompts
- [ ] Handshake completes within a reasonable time (<5s)

## Notes
- Check all three `_meta` scenarios from the architecture doc
- Pay attention to any differences in capability negotiation compared to OpenClaw
