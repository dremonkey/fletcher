use flake
PATH_add node_modules/.bin

# git-crypt + worktrees: use wrappers that symlink keys into worktree git dirs
git config filter.git-crypt.smudge "$(pwd)/scripts/git-crypt-smudge"
git config diff.git-crypt.textconv "$(pwd)/scripts/git-crypt-diff"
