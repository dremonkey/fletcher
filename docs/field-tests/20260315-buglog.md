# Fletcher Bug Log — 2026-03-15

**Date:** 2026-03-15
**Tester:** AH (Pixel 9)
**Monitor:** Claude (watching logs)

---

## Session Summary

| Time (UTC) | Severity | Description | Status |
|------------|----------|-------------|--------|
| 07:05 | HIGH | BUG-027: STT watchdog fires but session_hold lost to DTLS timeout | FIXED |
| 07:17 | HIGH | BUG-027b: Relay participant counted in disconnect handler, blocking hold flow | FIXED |
| 07:25 | LOW | First utterance after reconnect lost during bootstrap | KNOWN |

---

## Detailed Entries

### BUG-027a: session_hold lost to DTLS timeout (HIGH)

**First seen:** 07:17 UTC (room: `raw-hill`)
**Frequency:** Every BUG-027 recovery
**Impact:** Client never receives hold event, UI stays "connected", no recovery UX

**Log excerpt:**
```
[07:17:01] WARN STT watchdog: no STT activity detected — pipeline may be dead
           silenceMs: 33498, timeoutMs: 30000
```

**Analysis:** The watchdog sent `session_hold` at the 30s timeout via `disconnectRoom` callback, but the WebRTC data channels DTLS-timeout ~20s after the audio track unpublish. By the time the watchdog fires, the data channel is dead and the message is silently dropped.

**Fix applied:** Send `session_hold` early at the first 10s check interval while the transport is still alive. The actual disconnect still happens at 30s. Removed duplicate `session_hold` from `disconnectRoom` callback.

**Commit:** `62dff89`

---

### BUG-027b: Relay participant blocks disconnect handler (HIGH)

**First seen:** 07:25 UTC (room: `echo-spire`)
**Frequency:** Every session with relay present
**Impact:** Hold mode flow completely skipped — no status update, no `onAgentDisconnected`, no UI event

**Log excerpt:**
```
[Fletcher] Remote participant disconnected: agent-AJ_zCjXnuFDPnSQ (remaining=1)
[Ganglia] Session hold (reason: stt_watchdog)   ← received but not acted on
```

**Analysis:** `ParticipantDisconnectedEvent` handler counted all remote participants including the relay. When the agent left, `remaining=1` (relay still present), so the `if (remaining == 0)` guard blocked the entire hold mode flow: no `onAgentDisconnected()`, no status update, no UI event.

**Fix applied:** Changed `remaining` count to exclude relay participants (`identity.startsWith('relay-')`).

**Commit:** `cc3debd`

---

### First utterance lost during bootstrap (LOW)

**First seen:** 07:28 UTC (room: `echo-spire`, post-reconnect)
**Frequency:** Every reconnect
**Impact:** User's first words after tapping "resume" are not transcribed

**Analysis:** During the ~2s bootstrap phase (STT/VAD/TTS initialization), speech is not captured. The user speaks immediately on mic tap, but the agent isn't ready yet. Second utterance works fine. This is pre-existing behavior, not a regression from BUG-027 fixes.

**Status:** Known limitation — could be improved with a "ready" indicator or buffered audio.

---

## Notes

### Session 1 (~07:05 UTC)
- **Goal:** Confirm BUG-027 fix (STT watchdog) and TASK-069 (hold mode visual treatment).
- Room: `blaze-manor`
- Voice-agent rebuilt with STT watchdog (`stt-watchdog.ts`), TASK-069 hold event race fix.
- TTS mode: OFF (tester preference).

### Session 2 (~07:17 UTC)
- Room: `raw-hill`
- Watchdog fired correctly, but `session_hold` lost to DTLS timeout.
- Fixed with early send at 10s check interval.

### Session 3 (~07:25 UTC)
- Room: `echo-spire`
- Early `session_hold` delivered successfully (confirmed in logcat).
- But relay participant in `remaining` count blocked the disconnect handler.
- Fixed by excluding relay from count.
- **Result after fix:** Hold event shows in UI, mic tap triggers re-dispatch, agent reconnects, second utterance gets response. BUG-027 recovery flow confirmed working.
