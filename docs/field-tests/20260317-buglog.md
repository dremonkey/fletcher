# Fletcher Bug Log — 2026-03-17

**Date:** 2026-03-17
**Tester:** Andre
**Monitor:** Glitch (Scribe)

---

## Session Summary

| Time (UTC) | Severity | Description | Status |
|------------|----------|-------------|--------|
| 14:49 | HIGH | BUG-049: No network error on cold start; manual reconnect failed | RCA COMPLETE |
| 16:35 | MED | BUG-050: "Prompt timed out" UX error during long ACP tasks | FIXED |
| 16:39 | MED | BUG-051: Tool-call status not surfaced during relay chat | FIXED |
| 05:28 | HIGH | BUG-052: Network switching still drops sessions; short-lived rooms | OPEN |
| 05:28 | HIGH | BUG-053: Relay bind timeout after network switch; client can't bind | OPEN |
| 11:12 | HIGH | BUG-054: Relay shut down 3x with no auto-restart; 11min gap | OPEN |

---

## Detailed Entries

### BUG-054: Relay shut down 3x during session with no auto-restart (HIGH)

**First seen:** 11:12 UTC (04:12 PDT)
**Frequency:** 3 shutdowns across the day; worst gap was 11m 36s
**Impact:** Relay dead = no chat mode, no room joining, no ACP bridge. Tester stranded until manual restart.

**Log evidence — relay restarts:**
```
21:16:31 — Start (PID 1806416)
23:20:45 — Shutdown → 23:21:10 restart (PID 1909255)  [25s gap — OK]
11:12:42 — Shutdown → 11:24:18 restart (PID 2408729)  [11m 36s gap — BAD]
13:42:18 — Crash (no "Shutting down..." logged) → 13:44:26 restart (PID 2505198)  [2m 8s gap]
14:31:22 — Shutdown → 14:31:37 restart (PID 2554067)  [15s gap — OK]
```

**Analysis:** The relay runs as a bare `bun` process outside Docker with no supervisor, watchdog, or auto-restart. Graceful shutdowns are triggered by SIGINT/SIGTERM (see `apps/relay/src/index.ts:69-76`). The 11:12 shutdown was graceful (logged "Shutting down..."), meaning something sent it a signal. The 13:42 death was a crash — PID 2408729 disappeared without logging the shutdown handler, suggesting OOM, SIGKILL, or an unhandled exception that bypassed the signal handler.

All 42 ACP subprocesses required SIGKILL (SIGTERM ignored) — the ACP processes never shut down gracefully.

**Root cause:** No process supervision for the relay. When it dies, it stays dead.

**Fix needed:** Either:
1. Run the relay in Docker with `restart: unless-stopped`
2. Add a systemd unit / process manager
3. Add a watchdog/heartbeat that auto-restarts on death

**Status:** OPEN

---

### BUG-053: Relay bind timeout after network switch; client can't complete handshake (HIGH)

**First seen:** 09:29 UTC (02:29 PDT)
**Frequency:** 2 bind timeouts observed, plus 9 `session_load_error` events
**Impact:** Client connects to LiveKit room but relay can't bind session — no chat functionality.

**Log evidence:**
```
09:29:41  room_added_pending_bind   amber-elm-6v1d
09:30:11  bind_timeout              amber-elm-jh4c   [30s — no session/bind received]
09:30:12  room_added_pending_bind   amber-elm-jh4c   [retry]
09:30:42  bind_timeout              amber-elm-jh4c   [30s — failed again]
09:31:12  room_added_pending_bind   amber-elm-jh4c   [3rd attempt]
09:31:21  acp_spawn                 amber-elm-jh4c   [finally bound after ~2 minutes]
```

9 `session_load_error: "ACP subprocess not running"` events throughout the day indicate a race condition: the bridge tries to load session history before the ACP subprocess finishes spawning.

**Analysis:** The bind timeout happens when the relay joins a LiveKit room (via webhook/discovery) but the mobile client never sends the `session/bind` message within 30s. This can happen when:
1. The client is mid-network-switch and its data channel is broken
2. The client connected to LiveKit but the relay participant hasn't been discovered yet
3. The client app crashed/backgrounded during the handshake

The `session_load_error` race is separate — the bridge calls `session/load` on the ACP subprocess before it's ready. Needs a readiness gate.

**Status:** OPEN

---

### BUG-052: Network switching still drops sessions; ultra-short room lifetimes (HIGH) (TESTER REPORT)

**First seen:** Full-day observation
**Frequency:** Multiple instances throughout the day
**Impact:** Sessions last seconds instead of minutes after network transitions. User must re-open app repeatedly.

**Tester said:** "Network switch is still a problem."

**Log evidence — ultra-short rooms (< 60s):**
```
09:54:37 → 09:54:55  amber-elm-8n2u   [18 seconds]
13:25:42 → 13:26:03  amber-elm-le7h   [21 seconds]
13:45:08 → 13:45:29  amber-elm-pua8   [21 seconds]
19:57:15 → 19:57:54  amber-elm-fbe3   [39 seconds]
20:31:51 → 20:32:58  amber-elm-wg8s   [67 seconds]
```

**Analysis:** Despite fixes in task 094 (network switch mic grab + stuck room) and task 007 (WiFi→5G ICE renegotiation), network transitions still produce very short-lived sessions. The pattern is: client connects → room created → relay joins and bridges → client disconnects almost immediately → room torn down. This suggests:

1. The ICE renegotiation still fails on WiFi→cellular switches (stale candidate selected)
2. The LiveKit SDK's reconnect path may be choosing "new room" too aggressively instead of ICE restart
3. The ConnectivityService interface-change detection (added in task 094) may not fire fast enough to pause reconnect attempts until the new interface stabilizes

Client logs only cover 22:29-22:47 UTC (logcat buffer cleared at monitor start), so no client-side evidence for the earlier short-lived rooms.

**Related tasks:** `007-wifi-to-5g-ice-renegotiation.md` (closed), `094-network-switch-mic-grab-stuck-room.md` (closed), `011-network-transition-audio-track-timeout.md`

**Status:** OPEN — needs deeper client-side investigation during a future field test with logcat running from the start.

### BUG-051: Tool-call status not surfaced during relay chat (MED)

**First seen:** 09:39 PDT (16:39 UTC)
**Frequency:** Consistent during Chat Mode (text-only) tool use
**Impact:** "Black hole" UX during long processing; directly causes BUG-050 timeouts because the user/client has no visibility into progress.

**Symptoms:** When in text-mode (Chat Mode), tool-calls (reading, editing, etc.) do not emit status updates to the mobile UI like they do in Voice Mode. The user sees a blank state until the final response is delivered, triggering a timeout if the task is long.

**Analysis:** Voice Mode uses the `StatusBar` and `DiagnosticsBar` wired to data channel events from the voice-agent. Chat Mode routes through the Relay via JSON-RPC, and the Relay may not be forwarding interim `tool_call` or `status` notifications from the ACP subprocess to the mobile data channel. If we surface these, we can reset the client-side timeout on every "pulse" of activity.

**Root cause:** Two issues: (1) `negotiateSessionConfig()` in `relay-bridge.ts` looked up desired config values by `option.category`, but OpenClaw's `verbose_level` config option has no `category` field — so tool verbosity was never negotiated and remained `"off"`, meaning OpenClaw never emitted `tool_call`/`tool_call_update` events; (2) `acp-session-config.json` had no `verbose_level` entry. The `_meta: { verbose: true }` in `session/new` is opaque metadata and does not control OpenClaw's tool event emission. The mobile-side parser (`AcpUpdateParser`), UI (`ToolCallCard`), and relay forwarding were all already wired — the events simply never arrived.

**Fix:**
1. `relay-bridge.ts`: `negotiateSessionConfig()` falls back to `option.id` when `option.category` is absent (`const key = option.category ?? option.id`).
2. `acp-session-config.json`: Added `"verbose_level": "on"` under `"openclaw"`.
3. `relay_chat_service.dart`: Added `_resetPromptTimer()` — resets the 30s timeout on every valid incoming event (also fixes BUG-050).
4. `livekit_service.dart`: Passes `activeToolCalls: const []` at prompt start/complete/error to clear stale tool-call UI state.

**Status:** FIXED

### BUG-050: "Prompt timed out" UX error during long ACP tasks (MED)

**First seen:** 09:35 PDT (16:35 UTC)
**Frequency:** Observed during complex/long-running ACP tasks
**Impact:** Confusing UX; indicates failure when work is actually continuing in the background.

**Symptoms:** The mobile app displays a "Prompt timed out" error message even though the backend (Relay/ACP) is still processing the request.

**Analysis:** This is likely a client-side timeout in `RelayChatService.sendPrompt()` or a relay-side timeout that fires before the ACP subprocess finishes a heavy task (like Claude Code doing a repo-wide review). We recently added a 30s timeout to `sendPrompt()` in BUG-045 to fix ghost data channels, but that might be too aggressive for the "Chat Mode" use case where sub-agents can take minutes. 

**Root cause:** The 30s `promptTimeout` in `RelayChatService.sendPrompt()` was a flat wall-clock timer set once at prompt send. Tool-call and text-delta events arriving during processing never reset it, so multi-tool ACP tasks exceeding 30s total always triggered a false timeout — even though events were streaming in.

**Fix:** Resolved by BUG-051 fix. `_resetPromptTimer()` in `RelayChatService` converts the timeout to an *inactivity* timer: every valid incoming event (`tool_call`, `text_delta`, `thinking_delta`, `usage_update`, `user_message`) resets the 30s clock. The timeout only fires after 30s of silence.

**Status:** FIXED — addressed by BUG-051 fix.

### BUG-049: No network error on cold start; manual reconnect failed (HIGH)

**First seen:** 07:49 PDT (14:49 UTC)
**Frequency:** Observed once on cold start
**Impact:** App unusable on first launch. Manual reconnect button unresponsive.

**Symptoms:** User opened the app and immediately saw a "no network" error. Tapping the reconnect button did nothing. After a force-quit and relaunch, the app connected successfully.

**Task:** [`tasks/09-connectivity/096-cold-start-connection-failure.md`](../../tasks/09-connectivity/096-cold-start-connection-failure.md)

**Root cause:** Two compounding issues: (1) `connectWithDynamicRoom()` starts TCP-racing URLs ~1s after Dart VM boot, before Android's network routes are functional — all connections time out; (2) `tryReconnect()` requires cached `_url`/`_token` from `connect()`, but `connect()` was never reached, so tap-to-retry silently fails via null-credential guard in `_reconnectRoom()`.

**Status:** RCA COMPLETE — task 096 filed with fix plan.
