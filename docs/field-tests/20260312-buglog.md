# Fletcher Bug Log — Field Testing Session

**Date:** 2026-03-12
**Tester:** ahanyu (Pixel 9 device)
**Monitor:** Claude (watching logs, investigating issues)

---

## Session Summary

| Time (UTC) | Severity | Description | Status |
|------------|----------|-------------|--------|
| 21:05:42 | LOW | Device disconnect (prior to main session) | RESOLVED |
| 21:07:19 | HIGH | Chat message returns empty response — ACP schema mismatch | VERIFIED |

---

## Detailed Entries

### Background: Prior device disconnect

At 21:05:42, a device (Pixel 9) experienced `PEER_CONNECTION_DISCONNECTED` reason after 1m23s of connection. This appears to be from pre-session activity. Relay correctly handled the participant cleanup via webhook.

### BUG-001: Chat message returns empty response — ACP schema mismatch (HIGH)

**First seen:** 21:07:19 UTC
**Frequency:** Every chat message
**Impact:** User sends message in chat mode, mobile UI shows empty response box — content is flowing end-to-end but not rendered

**Log excerpt (relay debug, showing OpenClaw's actual format):**
```
"params": {
  "sessionId": "31ed0f39-...",
  "update": {
    "sessionUpdate": "agent_message_chunk",
    "content": { "type": "text", "text": "Copy that. I" }
  }
}
```

**Analysis:**
The relay was written expecting `params.updates[]` (array with `kind: "content_chunk"` items), but OpenClaw's ACP protocol sends `params.update` (singular object with `sessionUpdate: "agent_message_chunk"`). The mobile client calls `params['updates']` → gets null → defaults to `[]` → no content rendered. OpenClaw was responding correctly throughout; the mismatch was in our relay and mobile assumptions.

**Root cause:** Schema mismatch on our side — the relay + mobile were written against a designed protocol that didn't match OpenClaw's actual ACP format.

**Fix applied:** `apps/relay/src/bridge/relay-bridge.ts`
- Relay bridge `onUpdate` handler now translates from OpenClaw format before forwarding
- Filters non-content updates (`available_commands_update`, etc.)
- Maps `agent_message_chunk` → `{ updates: [{ kind: "content_chunk", content }] }`
- Updated `apps/relay/src/acp/types.ts` to reflect actual OpenClaw `session/update` schema
- Added debug logging in `acp/client.ts` and `relay-bridge.ts` (raw stdout, both directions)

**Status:** FIXED — verified ✅

**Client log context** (`20260312-client-1407-1501.txt`):
- `14:07:24` — first failing send: `Sending text message: 2 chars (mode=chat)` (empty box)
- `14:08:47` — second failing send: `Sending text message: 64 chars (mode=chat)` (empty box)
- `14:56:23` — send that triggered debug investigation: `41 chars (mode=chat)` (still broken)
- `15:00:54` — first post-fix send: `40 chars (mode=chat)` → **rendered correctly**
- `15:01:33` — verification send: `45 chars (mode=chat)` → **rendered correctly**

---

---

## Notes

- **Mode:** Chat mode testing (relay bridge to ACP backend)
- Command phrase: "Peanuts and Watermelons" = instruction from tester
- Services monitored: `livekit`, `voice-agent`, relay (7890) via `docker compose logs -f`
- Relay running: PID 1952718 (started 13:38)
- Session started: 2026-03-12 21:05 UTC (monitoring resumed)
- Ready for fresh chat test
